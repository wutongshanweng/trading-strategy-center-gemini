--- client/src/App.tsx
+++ client/src/App.tsx
@@ -53,6 +53,9 @@
   ArrowUpRight,
   ArrowDownRight,
+  Menu,
+  ChevronLeft,
+  ChevronRight
 } from 'lucide-react';
 import { DataCenterTab } from './components/DataCenterTab';
 import { ModuleHub } from './components/ModuleHub';
 
 export function App() {
   const [activeTab, setActiveTab] = useState<'datacenter' | 'modulehub' | 'decision' | 'pipeline' | 'backtest' | 'agent'>('datacenter');
+  const [isSidebarOpen, setIsSidebarOpen] = useState(true);
 
   // --- Multi-Timeframe & Decision State ---
@@ -348,82 +351,105 @@
   const marketStatus = diagnostics?.state?.marketSession;
 
   return (
-    <div className="min-h-screen bg-slate-900 text-slate-100 antialiased flex flex-col font-sans">
-      {/* Top Header & Market Clocks */}
-      <header className="bg-slate-950/90 backdrop-blur-md border-b border-slate-800 sticky top-0 z-40 px-6 lg:px-10 py-3 flex flex-wrap items-center justify-between gap-4">
-        <div className="flex items-center gap-3">
-          <div className="p-2.5 bg-linear-to-tr from-indigo-600 to-cyan-500 rounded-xl text-white shadow-md shadow-indigo-500/20">
-            <TrendingUp className="w-5 h-5" />
-          </div>
-          <div>
-            <div className="flex items-center gap-2">
-              <h1 className="text-base font-bold text-white tracking-tight">China Futures Quant Hub</h1>
-              <span className="px-2 py-0.5 text-[10px] font-semibold bg-emerald-500/10 text-emerald-400 border border-emerald-500/20 rounded-full flex items-center gap-1">
-                <span className="w-1.5 h-1.5 rounded-full bg-emerald-400 animate-pulse"></span>
-                中国期货专版 (只做决策研判)
-              </span>
-            </div>
-            <p className="text-xs text-slate-400">中金所 · 上期所 · 大商所 · 郑商所 · 广期所 | 多维聚合 & 决策系统</p>
+    <div className="flex h-screen bg-slate-900 text-slate-100 antialiased font-sans overflow-hidden">
+      {/* Sidebar */}
+      <aside className={`\${isSidebarOpen ? 'w-64' : 'w-16'} shrink-0 bg-slate-950/90 border-r border-slate-800 transition-all duration-300 flex flex-col z-50`}>
+        {/* Sidebar Header / Logo */}
+        <div className="h-16 shrink-0 flex items-center justify-between px-3 border-b border-slate-800">
+          <div className={`flex items-center gap-2 overflow-hidden transition-all duration-300 \${isSidebarOpen ? 'w-auto opacity-100' : 'w-0 opacity-0'}`}>
+            <div className="p-1.5 bg-linear-to-tr from-indigo-600 to-cyan-500 rounded-lg text-white shadow-md shadow-indigo-500/20 shrink-0">
+              <TrendingUp className="w-4 h-4" />
+            </div>
+            <h1 className="text-sm font-bold text-white whitespace-nowrap">China.Q</h1>
           </div>
+          <button 
+            onClick={() => setIsSidebarOpen(!isSidebarOpen)} 
+            className="p-1.5 rounded-lg hover:bg-slate-800 text-slate-400 hover:text-slate-200 transition-colors shrink-0"
+          >
+            {isSidebarOpen ? <ChevronLeft className="w-5 h-5" /> : <Menu className="w-5 h-5" />}
+          </button>
         </div>
 
-        {/* China Market Hours Status Badge */}
-        <div className="flex items-center gap-3 text-xs">
-          <div className={`flex items-center gap-2 px-3 py-1.5 rounded-xl border ${
-            marketStatus?.isOpen 
-              ? 'bg-emerald-950/40 border-emerald-600/50 text-emerald-300' 
-              : 'bg-slate-900 border-slate-800 text-slate-300'
-          }`}>
-            <Clock className={`w-4 h-4 ${marketStatus?.isOpen ? 'text-emerald-400 animate-pulse' : 'text-amber-400'}`} />
-            <div>
-              <div className="flex items-center gap-1.5">
-                <span className="font-bold">{marketStatus?.sessionName || '时段检测中...'}</span>
-                <span className="text-[10px] text-slate-400 font-mono">({marketStatus?.chinaTimeStr || '北京时间'})</span>
-              </div>
-              <p className="text-[10px] text-slate-400">{marketStatus?.nextSessionDesc || '暂无提醒'}</p>
-            </div>
-          </div>
-          <div className="hidden xl:flex items-center gap-1.5 px-3 py-1.5 bg-slate-900 border border-slate-800 rounded-xl text-slate-300">
-            <Database className="w-3.5 h-3.5 text-indigo-400" />
-            <span>PostgreSQL: <span className="text-indigo-300 font-bold font-mono">{diagnostics?.dbMetrics?.totalKlinesInDb ?? 0}</span> 条 K线 (已用 {diagnostics?.dbMetrics?.storageUsageEstimatedMb ?? 0}MB / 500MB 免费配额)</span>
-          </div>
-        </div>
-
-        {/* Tab Switcher */}
-        <div className="flex items-center bg-slate-900/90 p-1 rounded-xl border border-slate-800 flex-wrap gap-1">
+        {/* Sidebar Nav Items */}
+        <nav className="flex-1 overflow-y-auto py-4 px-2 space-y-1.5 overflow-x-hidden scrollbar-hide">
           <button
             onClick={() => setActiveTab('datacenter')}
-            className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold transition-all cursor-pointer ${
+            className={`w-full flex items-center gap-3 px-2 py-2.5 rounded-xl text-xs font-semibold transition-all cursor-pointer ${
               activeTab === 'datacenter' ? 'bg-indigo-600 text-white shadow-xs' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/50'
             }`}
+            title="数据中心"
           >
-            <Database className="w-3.5 h-3.5" />
-            <span>数据中心 (主力/10大品种/采集)</span>
+            <Database className="w-4 h-4 shrink-0" />
+            <span className={`whitespace-nowrap transition-all duration-300 \${isSidebarOpen ? 'opacity-100' : 'opacity-0 w-0 hidden'}`}>数据中心 (主力/采集)</span>
           </button>
+
           <button
             onClick={() => setActiveTab('modulehub')}
-            className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-bold transition-all cursor-pointer ${
+            className={`w-full flex items-center gap-3 px-2 py-2.5 rounded-xl text-xs font-bold transition-all cursor-pointer ${
               activeTab === 'modulehub' ? 'bg-gradient-to-r from-indigo-600 to-cyan-600 text-white shadow-xs' : 'text-cyan-300 hover:text-white hover:bg-slate-800/50'
             }`}
+            title="独立子系统"
           >
-            <Zap className="w-3.5 h-3.5 text-amber-300" />
-            <span>独立子系统 (因子/产业链/持仓风控)</span>
+            <Zap className="w-4 h-4 shrink-0 text-amber-300" />
+            <span className={`whitespace-nowrap transition-all duration-300 \${isSidebarOpen ? 'opacity-100' : 'opacity-0 w-0 hidden'}`}>子系统 (因子/风控)</span>
           </button>
+
           <button
             onClick={() => setActiveTab('decision')}
-            className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold transition-all cursor-pointer ${
+            className={`w-full flex items-center gap-3 px-2 py-2.5 rounded-xl text-xs font-semibold transition-all cursor-pointer ${
               activeTab === 'decision' ? 'bg-indigo-600 text-white shadow-xs' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/50'
             }`}
+            title="多维K线"
           >
-            <Compass className="w-3.5 h-3.5" />
-            <span>多维 K线与交易决策</span>
+            <Compass className="w-4 h-4 shrink-0" />
+            <span className={`whitespace-nowrap transition-all duration-300 \${isSidebarOpen ? 'opacity-100' : 'opacity-0 w-0 hidden'}`}>K线与交易决策</span>
           </button>
+
           <button
             onClick={() => setActiveTab('pipeline')}
-            className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold transition-all cursor-pointer ${
+            className={`w-full flex items-center gap-3 px-2 py-2.5 rounded-xl text-xs font-semibold transition-all cursor-pointer ${
               activeTab === 'pipeline' ? 'bg-indigo-600 text-white shadow-xs' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/50'
             }`}
+            title="存储聚合引擎"
           >
-            <Layers className="w-3.5 h-3.5" />
-            <span>存储与聚合引擎</span>
+            <Layers className="w-4 h-4 shrink-0" />
+            <span className={`whitespace-nowrap transition-all duration-300 \${isSidebarOpen ? 'opacity-100' : 'opacity-0 w-0 hidden'}`}>存储聚合引擎</span>
           </button>
+
           <button
             onClick={() => setActiveTab('backtest')}
-            className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold transition-all cursor-pointer ${
+            className={`w-full flex items-center gap-3 px-2 py-2.5 rounded-xl text-xs font-semibold transition-all cursor-pointer ${
               activeTab === 'backtest' ? 'bg-indigo-600 text-white shadow-xs' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/50'
             }`}
+            title="策略回测"
           >
-            <Trophy className="w-3.5 h-3.5" />
-            <span>策略回测与赛马</span>
+            <Trophy className="w-4 h-4 shrink-0" />
+            <span className={`whitespace-nowrap transition-all duration-300 \${isSidebarOpen ? 'opacity-100' : 'opacity-0 w-0 hidden'}`}>策略回测赛马</span>
           </button>
+
           <button
             onClick={() => setActiveTab('agent')}
-            className={`flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-semibold transition-all cursor-pointer ${
+            className={`w-full flex items-center gap-3 px-2 py-2.5 rounded-xl text-xs font-semibold transition-all cursor-pointer ${
               activeTab === 'agent' ? 'bg-indigo-600 text-white shadow-xs' : 'text-slate-400 hover:text-slate-200 hover:bg-slate-800/50'
             }`}
+            title="AI投研"
           >
-            <Sparkles className="w-3.5 h-3.5" />
-            <span>AI 深度投研</span>
+            <Sparkles className="w-4 h-4 shrink-0" />
+            <span className={`whitespace-nowrap transition-all duration-300 \${isSidebarOpen ? 'opacity-100' : 'opacity-0 w-0 hidden'}`}>AI 深度投研</span>
           </button>
-        </div>
-      </header>
+        </nav>
+      </aside>
 
-      {/* Main Container - Full-width spacious canvas */}
-      <main className="flex-1 max-w-[2240px] w-full mx-auto p-4 md:p-6 lg:p-8 xl:p-10 space-y-6">
+      {/* Main Content Area */}
+      <div className="flex-1 flex flex-col h-screen overflow-hidden">
+        
+        {/* Top Header for Market Status & App Title */}
+        <header className="shrink-0 h-16 bg-slate-950/50 backdrop-blur-md border-b border-slate-800 px-4 md:px-6 flex items-center justify-between gap-4">
+          <div className="flex flex-col">
+            <h1 className="text-sm font-bold text-white tracking-tight">China Futures Quant Hub</h1>
+            <p className="text-[10px] text-slate-400">中金所 · 上期所 · 大商所 · 郑商所 · 广期所 | 多维聚合 & 决策系统</p>
+          </div>
+
+          <div className="flex items-center gap-4 text-xs">
+            <div className={`flex items-center gap-2 px-3 py-1.5 rounded-xl border \${
+              marketStatus?.isOpen 
+                ? 'bg-emerald-950/40 border-emerald-600/50 text-emerald-300' 
+                : 'bg-slate-900 border-slate-800 text-slate-300'
+            }`}>
+              <Clock className={`w-4 h-4 \${marketStatus?.isOpen ? 'text-emerald-400 animate-pulse' : 'text-amber-400'}`} />
+              <div className="hidden sm:block">
+                <div className="flex items-center gap-1.5">
+                  <span className="font-bold">{marketStatus?.sessionName || '时段检测中...'}</span>
+                  <span className="text-[10px] text-slate-400 font-mono">({marketStatus?.chinaTimeStr || '北京时间'})</span>
+                </div>
+                <p className="text-[10px] text-slate-400">{marketStatus?.nextSessionDesc || '暂无提醒'}</p>
+              </div>
+            </div>
+            
+            <div className="hidden lg:flex items-center gap-1.5 px-3 py-1.5 bg-slate-900 border border-slate-800 rounded-xl text-slate-300">
+              <Database className="w-3.5 h-3.5 text-indigo-400" />
+              <span className="whitespace-nowrap">PostgreSQL: <span className="text-indigo-300 font-bold font-mono">{diagnostics?.dbMetrics?.totalKlinesInDb ?? 0}</span> 条 K线</span>
+            </div>
+          </div>
+        </header>
+
+        {/* Main Canvas Container */}
+        <main className="flex-1 overflow-y-auto p-4 md:p-6 lg:p-8 space-y-6">
 
         {/* ======================================================== */}
         {/* TAB 0: DATA CENTER (CONTRACT LIFECYCLE & INGESTION)      */}
